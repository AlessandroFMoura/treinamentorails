<!DOCTYPE html>
<html>
  <head>
    <title>Treinamentorails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>

    <p class="navbar-text float-right">
    <% if admin_signed_in? %>
      Logado como: <strong><%= current_admin.email %></strong>.<br>
      Matricula: <strong><%= current_admin.matricula %></strong>.
      <%= link_to 'Editar perfil', edit_admin_registration_path, :class => 'navbar-link' %> |
      <%= button_to "Logout", destroy_admin_session_path, method: :delete, :class => 'navbar-link'  %>
    <% else %>
      <%= link_to "Registrar-se", new_admin_registration_path, :class => 'navbar-link'  %> |
      <%= link_to "Login", new_admin_session_path, :class => 'navbar-link'  %>
    <% end %>
    <%= yield %>
  </body>
</html>
